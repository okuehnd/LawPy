services:
  mongodb:
    build:
      context: .
      dockerfile: Dockerfile.mongodb
    container_name: lawpy-mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/import
      - mongodb_data:/data/db
    networks:
      - lawpy-network
    restart: unless-stopped

  # Frontend service - commented out for now
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./frontend:/app
  #   depends_on:
  #     - mongodb
  #   networks:
  #     - lawpy-network
  #   environment:
  #     - MONGODB_URI=mongodb://mongodb:27017/lawpy

networks:
  lawpy-network:
    driver: bridge

volumes:
  mongodb_data:
    driver: local 